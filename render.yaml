services:
  # WebSocket 服务器
  - type: web
    name: websocket-chat-server
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /

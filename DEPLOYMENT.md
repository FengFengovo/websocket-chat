# WebSocket 聊天应用部署指南

## 🚀 快速部署到 Render（免费）

### 前置准备
1. 注册 [Render](https://render.com) 账号（免费，无需信用卡）
2. 将代码推送到 GitHub 仓库

### 部署步骤

#### 1. 推送代码到 GitHub
```bash
# 初始化 Git 仓库（如果还没有）
git init

# 添加所有文件
git add .

# 提交
git commit -m "准备部署到Render"

# 关联远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/websocket-chat.git

# 推送代码
git push -u origin main
```

#### 2. 在 Render 创建 Web Service

1. 登录 [Render Dashboard](https://dashboard.render.com/)
2. 点击 **"New +"** → 选择 **"Web Service"**
3. 连接你的 GitHub 仓库
4. 配置如下：

**基本设置：**
- **Name**: `websocket-chat-server`（或任意名称）
- **Environment**: `Node`
- **Region**: 选择离你最近的区域（如 Singapore）
- **Branch**: `main`

**构建设置：**
- **Build Command**: `npm install`
- **Start Command**: `npm start`

**实例类型：**
- 选择 **Free** 免费套餐

5. 点击 **"Create Web Service"**

#### 3. 等待部署完成

- Render 会自动构建和部署你的应用
- 部署完成后，你会得到一个 URL，类似：`https://websocket-chat-server.onrender.com`

#### 4. 更新前端 WebSocket 地址

部署完成后，需要修改前端代码中的 WebSocket 地址：

打开 `src/hooks/useWebSocket.js`，将：
```javascript
const ws = new WebSocket('ws://localhost:3001');
```

改为：
```javascript
const ws = new WebSocket('wss://你的应用名称.onrender.com');
```

**注意：** 使用 `wss://`（安全连接），而不是 `ws://`

#### 5. 重新部署前端

提交更改并推送：
```bash
git add .
git commit -m "更新WebSocket地址"
git push
```

Render 会自动重新部署。

---

## 📝 重要说明

### 免费版限制
- ✅ 支持 WebSocket
- ✅ 自动 HTTPS
- ⚠️ 15分钟无活动会休眠
- ⚠️ 每月 750 小时免费运行时间

### 防止休眠（可选）
免费版应用在15分钟无活动后会休眠。可以使用以下方法保持活跃：

1. **使用 UptimeRobot**（推荐）
   - 注册 [UptimeRobot](https://uptimerobot.com/)
   - 添加监控，每5分钟 ping 一次你的应用

2. **使用 Cron-job.org**
   - 注册 [Cron-job.org](https://cron-job.org/)
   - 创建定时任务，每10分钟访问一次

---

## 🔧 本地开发

### 启动服务器
```bash
npm run server
```

### 启动前端开发服务器
```bash
npm run dev
```

---

## 🌐 其他部署选项

### Railway（推荐，$5/月）
1. 访问 [Railway](https://railway.app/)
2. 连接 GitHub 仓库
3. 自动检测并部署
4. 优点：不会休眠，性能更好

### Fly.io（免费额度）
1. 安装 Fly CLI
2. 运行 `fly launch`
3. 按提示完成部署

### Heroku（付费）
1. 安装 Heroku CLI
2. 运行 `heroku create`
3. 推送代码：`git push heroku main`

---

## 📞 技术支持

如遇到问题，请检查：
1. WebSocket 地址是否正确（wss:// 而不是 ws://）
2. Render 日志中是否有错误
3. 浏览器控制台是否有错误信息

---

## 🎉 部署成功！

部署完成后，你可以：
- 分享应用 URL 给朋友
- 创建房间并邀请他人加入
- 享受实时聊天和文件传输功能
